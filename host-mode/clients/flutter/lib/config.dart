class Config {
  /// Host backend base URL — auto-configured by start.sh
  /// Simulator/emulator: localhost. Physical device: LAN IP.
  static const String hostServerURL = 'http://192.168.1.185:8765';
}
