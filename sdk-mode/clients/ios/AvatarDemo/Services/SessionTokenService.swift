import Foundation

/// Compatibility note: iOS sample uses manual Session Token input and does not fetch from backend.
enum SessionTokenService {
    static let temporaryTokenDocsURL = "https://docs.spatialreal.ai/overview/get-apikeys#temporary-session-token"
}
